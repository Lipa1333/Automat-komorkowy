#pragma once

#include <vector>
#include <list>
#include <list>
using namespace std;
class Baza
{
public:
	void zapisz(string nazwa_pliku, Pole pole);
	void wczytaj(string nazwa_pliku);

};
